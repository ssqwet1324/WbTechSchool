# 📅 Event Booking Service

Сервис для создания мероприятий, бронирования мест и автоматического управления резервированиями. Поддерживает подтверждение платежей и автоматическую отмену неоплаченных броней.

## 🚀 Возможности

- **Создание мероприятий** - создание событий с указанием даты, названия и количества мест
- **Система мест** - гибкая сетка мест (ряды, места в ряду, нумерация)
- **Бронирование мест** - резервирование мест пользователями
- **Подтверждение платежей** - оплата забронированного места
- **Автоматическая отмена** - удаление неподтвержденных броней через заданный интервал времени
- **Управление статусами** - отслеживание статуса каждого места (свободно, забронировано, оплачено)
- **Транзакции БД** - безопасная работа с данными, избежание race conditions
- **Асинхронная обработка** - фоновый процесс для управления просроченными бронями

## 🛠️ Технологии

### Backend
- **Gin** - HTTP веб-фреймворк
- **PostgreSQL** - база данных для хранения событий и броней
- **Сron** — планировщик задач для очистки броней
- **Goroutines** - асинхронная обработка и планирование задач

### Инфраструктура
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация сервисов

## 📡 API Endpoints

### 1. Создание мероприятия
```http
POST /events
Content-Type: application/json

{
  "event": {
    "id": "event-001",
    "title": "Summer Concert 2024",
    "date": "2024-07-15T20:00:00Z",
    "total_seats": 1000
  },
  "layout": {
    "rows": 10,
    "seats_per_row": 100,
    "start_number": 1
  }
}
```

**Пример ответа:**
```json
{
  "event_id": "fc026d65-2bec-44a2-aef6-ee7d106fcc68"
}
```

### 2. Получение информации о мероприятии
```http
GET /events/{id}

# Пример:
GET /events/event-001
```

**Пример ответа:**
```json
{
  "id": "event-001",
  "title": "Summer Concert 2024",
  "date": "2024-07-15T20:00:00Z",
  "total_seats": 1000,
  "seats": [
    {
      "seat_number": 1,
      "status": "available"
    },
    {
      "seat_number": 2,
      "status": "booked"
    },
    {
      "seat_number": 3,
      "status": "confirmed"
    }
  ]
}
```

### 3. Получение всех мероприятий
```http
GET /events/all
```

**Пример ответа:**
```json
[
  {
    "id": "event-001",
    "title": "Summer Concert 2024",
    "date": "2024-07-15T20:00:00Z",
    "total_seats": 1000
  },
  {
    "id": "event-002",
    "title": "Master Class",
    "date": "2024-08-20T18:00:00Z",
    "total_seats": 50
  }
]
```

### 4. Бронирование места
```http
POST /events/{id}/book
Content-Type: application/json

{
  "user_id": "user-123",
  "seat_number": 42
}

# Пример:
POST /events/event-001/book
```

**Пример ответа:**
```json
{
  "event_id": "fc026d65-2bec-44a2-aef6-ee7d106fcc68",
  "message": "seat reserved successfully",
  "seat_number": 23
}
```

### 5. Подтверждение платежа (оплата брони)
```http
POST /events/{id}/confirm
Content-Type: application/json

{
  "user_id": "user-123",
  "seat_number": 42
}

# Пример:
POST /events/event-001/confirm
```

**Пример ответа:**
```json
{
  "event_id": "fc026d65-2bec-44a2-aef6-ee7d106fcc68",
  "message": "booking confirmed successfully"
}
```

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd event-booking-service
```

### 2. Запустите сервисы через Docker Compose:
```bash
docker-compose up -d
```

### 3. Откройте API:
```
http://localhost:8081
```

## 🔧 Разработка

### 1. Установите зависимости:
```bash
go mod download
```

### 2. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

### 3. Выполните миграции БД:
```bash
go run migrations/main.go
```

### 4. Запустите сервис:
```bash
go run cmd/event_booking/main.go
```